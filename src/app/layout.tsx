import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "next-themes";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Vehicle Health Evaluation System",
  description: "AI-powered vehicle health monitoring with real-time diagnostics, predictive maintenance, and fleet management.",
  keywords: ["Vehicle Health", "Fleet Management", "Diagnostics", "AI", "Predictive Maintenance"],
  authors: [{ name: "Vehicle Health Team" }],
  icons: {
    icon: "/pic_vit.png",
  },
  openGraph: {
    title: "Vehicle Health Evaluation System",
    description: "AI-powered vehicle health monitoring and fleet management",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
